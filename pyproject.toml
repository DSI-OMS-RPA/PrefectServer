[project]
name = "prefectserver"
version = "0.1.0"
description = "Prefect server for CVT/DSI ETL projects"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "prefect[github]>=3.3.4",
]
